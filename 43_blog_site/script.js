// Blog stub: load posts from local data
const posts = [{id:1,title:'Welcome to the blog',excerpt:'First post excerpt...'}];
console.log('Posts count:', posts.length);
// Later: fetch posts from API and render into DOM
